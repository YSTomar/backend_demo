<div>
  Chatting with <%= conversation.with(current_user).name %>
  <br>
  <em><%= conversation.messages.last.body.truncate(50) rescue nil%></em>
  <br>
  <%= link_to 'View conversation', conversation_path(conversation) %>
  <hr>
</div>
