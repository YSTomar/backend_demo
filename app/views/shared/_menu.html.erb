<nav class="navbar navbar-inverse">
  <%= link_to 'Backend Demo', root_path, class: 'navbar-brand' %>

  <% if user_signed_in? %>
    <div id="current-user"></div>
    <ul class="nav navbar-nav pull-xs-right">
      <li class="nav-item">
        <span class="text-muted">Logged in as <strong><%= current_user.name %></strong> | <%= link_to 'Log out', destroy_user_session_path, method: :delete %></span> 
      </li>
    </ul>
  <% end %>
</nav>
